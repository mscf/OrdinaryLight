# Historical artifacts

This directory contains captures retained from renderer development. New gate
and benchmark output should be written outside the source tree (the formal gate
defaults use `/tmp`). These files are evidence, not runtime package data.
